/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.egain.sdk;

import static com.egain.sdk.operations.Operations.RequestOperation;

import com.egain.sdk.models.operations.GetpopulararticlesRequest;
import com.egain.sdk.models.operations.GetpopulararticlesRequestBuilder;
import com.egain.sdk.models.operations.GetpopulararticlesResponse;
import com.egain.sdk.operations.Getpopulararticles;
import com.egain.sdk.utils.Headers;
import jakarta.annotation.Nonnull;


public class Populararticles {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final AsyncPopulararticles asyncSDK;

    Populararticles(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.asyncSDK = new AsyncPopulararticles(this, sdkConfiguration);
    }

    /**
     * Switches to the async SDK.
     * 
     * @return The async SDK
     */
    public AsyncPopulararticles async() {
        return asyncSDK;
    }

    /**
     * Get Popular Articles
     * 
     * <p>## Overview
     * The Popular Articles API allows a user to retrieve popular articles.
     * ## Prerequisites
     * * Only displayable articles are returned. An Article is displayable when "Include in browse on
     * portals" property is enable for the Article.
     * ## Permissions
     * * Agent permissions: The following permissions are required if user is an Agent:
     * * If Article has Access Tags, Article must be available for agent's current user profile.
     * * If Article has Publish Views, at least one edition of Article must be available for agent's
     * current user profile.
     * * If Article has filters and tags query parameter provided, Article filters must match provided tags
     * or tag groups.
     * * Customer permissions: The following permissions are required if user is a Customer:
     * * If Article has Access tags:
     * * Portal must have default user profile, and;
     * * Article must be available for portal's default user profile.
     * * If Article has Publish Views:
     * * Portal must have default user profile, and;
     * * At least one edition must be available for portal's default user profile.
     * *  If Article has filters and tags query parameter provided, Article filters must match provided
     * tags or tag groups.
     * 
     * @return The call builder
     */
    public GetpopulararticlesRequestBuilder getpopulararticles() {
        return new GetpopulararticlesRequestBuilder(sdkConfiguration);
    }

    /**
     * Get Popular Articles
     * 
     * <p>## Overview
     * The Popular Articles API allows a user to retrieve popular articles.
     * ## Prerequisites
     * * Only displayable articles are returned. An Article is displayable when "Include in browse on
     * portals" property is enable for the Article.
     * ## Permissions
     * * Agent permissions: The following permissions are required if user is an Agent:
     * * If Article has Access Tags, Article must be available for agent's current user profile.
     * * If Article has Publish Views, at least one edition of Article must be available for agent's
     * current user profile.
     * * If Article has filters and tags query parameter provided, Article filters must match provided tags
     * or tag groups.
     * * Customer permissions: The following permissions are required if user is a Customer:
     * * If Article has Access tags:
     * * Portal must have default user profile, and;
     * * Article must be available for portal's default user profile.
     * * If Article has Publish Views:
     * * Portal must have default user profile, and;
     * * At least one edition must be available for portal's default user profile.
     * *  If Article has filters and tags query parameter provided, Article filters must match provided
     * tags or tag groups.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public GetpopulararticlesResponse getpopulararticles(@Nonnull GetpopulararticlesRequest request) {
        RequestOperation<GetpopulararticlesRequest, GetpopulararticlesResponse> operation
              = new Getpopulararticles.Sync(sdkConfiguration, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
