description = 'egain-api Spring Boot Starter'

// Publishing configuration overrides
ext {
    publishingConfig = [
        groupId: rootProject.group,
        artifactId: 'egain-api-spring-boot-starter',
        version: rootProject.version,
        name: 'eGain Corporation Java SDK - Spring Boot Starter',
        description: 'Spring Boot starter for the eGain Corporation Java SDK.',
    ]
}

dependencies {
    api project(':')
    api project(':egain-api-spring-boot-autoconfigure')
    api "org.springframework.boot:spring-boot-starter:${rootProject.springBootVersion}"
}

jar {
    dependsOn 'generatePomFileForMavenPublication'
    archiveBaseName = 'egain-api-spring-boot-starter'
    into("META-INF/maven/com.egain.sdk/egain-api-spring-boot-starter") {
        from(layout.buildDirectory.file("pom.xml")) {
            // Only include if it exists to avoid build ordering issues
            duplicatesStrategy = DuplicatesStrategy.INCLUDE
        }
    }
}